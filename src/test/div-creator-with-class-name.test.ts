const { DivCreatorWithClassName } = require('../form-element-creator/div-creator')

describe('DivCreatorWithClassName', () => {
    it('create div element with class name', () => {
        // given
        const DivCreatorWithClassNameInstance = new DivCreatorWithClassName();

        // when then
        expect(DivCreatorWithClassNameInstance.createDiv('container').tagName).toBe('DIV');
        expect(DivCreatorWithClassNameInstance.createDiv('container').className).toBe('container');
    });

    it('create div element with class name', () => {
        // given
        const DivCreatorWithClassNameInstance = new DivCreatorWithClassName();

        // when then
        expect(DivCreatorWithClassNameInstance.createDiv('form-step').tagName).toBe('DIV');
        expect(DivCreatorWithClassNameInstance.createDiv('form-step').className).toBe('form-step');
    });

    it('create div element with class name', () => {
        // given
        const DivCreatorWithClassNameInstance = new DivCreatorWithClassName();

        // when then
        expect(DivCreatorWithClassNameInstance.createDiv('error-phone').tagName).toBe('DIV');
        expect(DivCreatorWithClassNameInstance.createDiv('error-phone').className).toBe('error-phone');
    });

    it('create div element with class name', () => {
        // given
        const DivCreatorWithClassNameInstance = new DivCreatorWithClassName();

        // when then
        expect(DivCreatorWithClassNameInstance.createDiv('required').tagName).toBe('DIV');
        expect(DivCreatorWithClassNameInstance.createDiv('required').className).toBe('required');
    });

    it('create div element with class name', () => {
        // given
        const DivCreatorWithClassNameInstance = new DivCreatorWithClassName();

        // when then
        expect(DivCreatorWithClassNameInstance.createDiv('text').tagName).toBe('DIV');
        expect(DivCreatorWithClassNameInstance.createDiv('text').className).toBe('text');
    });

    it('create div element with class name', () => {
        // given
        const DivCreatorWithClassNameInstance = new DivCreatorWithClassName();

        // when then
        expect(DivCreatorWithClassNameInstance.createDiv('popup-container').tagName).toBe('DIV');
        expect(DivCreatorWithClassNameInstance.createDiv('popup-container').className).toBe('popup-container');
    });

    it('create div element with class name', () => {
        // given
        const DivCreatorWithClassNameInstance = new DivCreatorWithClassName();

        // when then
        expect(DivCreatorWithClassNameInstance.createDiv('success-message-paragraph').tagName).toBe('DIV');
        expect(DivCreatorWithClassNameInstance.createDiv('success-message-paragraph').className).toBe('success-message-paragraph');
    });

    it('create div element with class name', () => {
        // given
        const DivCreatorWithClassNameInstance = new DivCreatorWithClassName();

        // when then
        expect(DivCreatorWithClassNameInstance.createDiv('input-data').tagName).toBe('DIV');
        expect(DivCreatorWithClassNameInstance.createDiv('input-data').className).toBe('input-data');
    });

    it('create div element with class name', () => {
        // given
        const DivCreatorWithClassNameInstance = new DivCreatorWithClassName();

        // when then
        expect(DivCreatorWithClassNameInstance.createDiv('input-data-gender').tagName).toBe('DIV');
        expect(DivCreatorWithClassNameInstance.createDiv('input-data-gender').className).toBe('input-data-gender');
    });

    it('create div element with class name', () => {
        // given
        const DivCreatorWithClassNameInstance = new DivCreatorWithClassName();

        // when then
        expect(DivCreatorWithClassNameInstance.createDiv('gender-label').tagName).toBe('DIV');
        expect(DivCreatorWithClassNameInstance.createDiv('gender-label').className).toBe('gender-label');
    });

    it('create div element with class name', () => {
        // given
        const DivCreatorWithClassNameInstance = new DivCreatorWithClassName();

        // when then
        expect(DivCreatorWithClassNameInstance.createDiv('showPass').tagName).toBe('DIV');
        expect(DivCreatorWithClassNameInstance.createDiv('showPass').className).toBe('showPass');
    });

    it('create div element with class name', () => {
        // given
        const DivCreatorWithClassNameInstance = new DivCreatorWithClassName();

        // when then
        expect(DivCreatorWithClassNameInstance.createDiv('button-container').tagName).toBe('DIV');
        expect(DivCreatorWithClassNameInstance.createDiv('button-container').className).toBe('button-container');
    });

    it('create div element with class name', () => {
        // given
        const DivCreatorWithClassNameInstance = new DivCreatorWithClassName();

        // when then
        expect(DivCreatorWithClassNameInstance.createDiv('input-data-pass').tagName).toBe('DIV');
        expect(DivCreatorWithClassNameInstance.createDiv('input-data-pass').className).toBe('input-data-pass');
    });

    it('create div element with class name', () => {
        // given
        const DivCreatorWithClassNameInstance = new DivCreatorWithClassName();

        // when then
        expect(DivCreatorWithClassNameInstance.createDiv('input-data-phone').tagName).toBe('DIV');
        expect(DivCreatorWithClassNameInstance.createDiv('input-data-phone').className).toBe('input-data-phone');
    });

    it('create div element with class name', () => {
        // given
        const DivCreatorWithClassNameInstance = new DivCreatorWithClassName();

        // when then
        expect(DivCreatorWithClassNameInstance.createDiv('input-group').tagName).toBe('DIV');
        expect(DivCreatorWithClassNameInstance.createDiv('input-group').className).toBe('input-group');
    });

    it('create div element with class name', () => {
        // given
        const DivCreatorWithClassNameInstance = new DivCreatorWithClassName();

        // when then
        expect(DivCreatorWithClassNameInstance.createDiv('phoneCountryCodeSelect').tagName).toBe('DIV');
        expect(DivCreatorWithClassNameInstance.createDiv('phoneCountryCodeSelect').className).toBe('phoneCountryCodeSelect');
    });

    it('create div element with class name', () => {
        // given
        const DivCreatorWithClassNameInstance = new DivCreatorWithClassName();

        // when then
        expect(DivCreatorWithClassNameInstance.createDiv('step-indicator-container-top').tagName).toBe('DIV');
        expect(DivCreatorWithClassNameInstance.createDiv('step-indicator-container-top').className).toBe('step-indicator-container-top');
    });

    it('create div element with class name', () => {
        // given
        const DivCreatorWithClassNameInstance = new DivCreatorWithClassName();

        // when then
        expect(DivCreatorWithClassNameInstance.createDiv('step-indicator-wrapper').tagName).toBe('DIV');
        expect(DivCreatorWithClassNameInstance.createDiv('step-indicator-wrapper').className).toBe('step-indicator-wrapper');
    });

    it('create div element with class name', () => {
        // given
        const DivCreatorWithClassNameInstance = new DivCreatorWithClassName();

        // when then
        expect(DivCreatorWithClassNameInstance.createDiv('step-indicator').tagName).toBe('DIV');
        expect(DivCreatorWithClassNameInstance.createDiv('step-indicator').className).toBe('step-indicator');
    });

    it('create div element with class name', () => {
        // given
        const DivCreatorWithClassNameInstance = new DivCreatorWithClassName();

        // when then
        expect(DivCreatorWithClassNameInstance.createDiv('step-title').tagName).toBe('DIV');
        expect(DivCreatorWithClassNameInstance.createDiv('step-title').className).toBe('step-title');
    });

    it('create div element with class name', () => {
        // given
        const DivCreatorWithClassNameInstance = new DivCreatorWithClassName();

        // when then
        expect(DivCreatorWithClassNameInstance.createDiv('green-line').tagName).toBe('DIV');
        expect(DivCreatorWithClassNameInstance.createDiv('green-line').className).toBe('green-line');
    });

    it('create div element with class name', () => {
        // given
        const DivCreatorWithClassNameInstance = new DivCreatorWithClassName();

        // when then
        expect(DivCreatorWithClassNameInstance.createDiv('step-indicator-line').tagName).toBe('DIV');
        expect(DivCreatorWithClassNameInstance.createDiv('step-indicator-line').className).toBe('step-indicator-line');
    });

    it('create div element with class name', () => {
        // given
        const DivCreatorWithClassNameInstance = new DivCreatorWithClassName();

        // when then
        expect(DivCreatorWithClassNameInstance.createDiv('current-step').tagName).toBe('DIV');
        expect(DivCreatorWithClassNameInstance.createDiv('current-step').className).toBe('current-step');
    });

    it('create div element with class name', () => {
        // given
        const DivCreatorWithClassNameInstance = new DivCreatorWithClassName();

        // when then
        expect(DivCreatorWithClassNameInstance.createDiv('personal-data-form').tagName).toBe('DIV');
        expect(DivCreatorWithClassNameInstance.createDiv('personal-data-form').className).toBe('personal-data-form');
    });

    it('create div element with class name', () => {
        // given
        const DivCreatorWithClassNameInstance = new DivCreatorWithClassName();

        // when then
        expect(DivCreatorWithClassNameInstance.createDiv('previous-button').tagName).toBe('DIV');
        expect(DivCreatorWithClassNameInstance.createDiv('previous-button').className).toBe('previous-button');
    });

    it('create div element with class name', () => {
        // given
        const DivCreatorWithClassNameInstance = new DivCreatorWithClassName();

        // when then
        expect(DivCreatorWithClassNameInstance.createDiv('next-button').tagName).toBe('DIV');
        expect(DivCreatorWithClassNameInstance.createDiv('next-button').className).toBe('next-button');
    });

    it('create div element with class name', () => {
        // given
        const DivCreatorWithClassNameInstance = new DivCreatorWithClassName();

        // when then
        expect(DivCreatorWithClassNameInstance.createDiv('empty-effect').tagName).toBe('DIV');
        expect(DivCreatorWithClassNameInstance.createDiv('empty-effect').className).toBe('empty-effect');
    });

    it('create div element with class name', () => {
        // given
        const DivCreatorWithClassNameInstance = new DivCreatorWithClassName();

        // when then
        expect(DivCreatorWithClassNameInstance.createDiv('form-container').tagName).toBe('DIV');
        expect(DivCreatorWithClassNameInstance.createDiv('form-container').className).toBe('form-container');
    })
});
